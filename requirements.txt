# ---------------------------------------------------------------------------
# ROOT requirements — what a DEPLOYMENT installs.
#
# Streamlit Community Cloud reads this file automatically and offers no way to
# point at a different one, so it must contain the app's dependencies and
# nothing heavier. The deployed app never touches geopandas, fiona, psycopg2 or
# scikit-learn: it reads the pre-simplified snapshot geojson as plain JSON and
# falls back to parquet when no database is reachable.
#
# Leaving the geospatial stack in here drags in GDAL system libraries, which is
# the single most common reason a Streamlit Cloud build fails.
#
# To run the full PIPELINE (database, crosswalk, optimiser, ML), install
# requirements-pipeline.txt instead — `make venv` does this for you.
# ---------------------------------------------------------------------------

streamlit==1.36.0
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0
plotly==5.22.0
PuLP==2.8.0
PyYAML==6.0.1
python-dotenv==1.0.1
SQLAlchemy==2.0.31
