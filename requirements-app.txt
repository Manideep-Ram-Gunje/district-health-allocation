# Deployment requirements — the Streamlit app ONLY.
#
# Deliberately smaller than requirements.txt. The deployed app never touches
# geopandas, fiona or psycopg2: it reads the pre-simplified snapshot geojson as
# plain JSON, and falls back to parquet when no database is reachable. Dropping
# the geospatial stack removes the GDAL system dependency, which is the usual
# reason a Streamlit Cloud build fails.
#
# Use this file for deployment; use requirements.txt to run the pipeline.

streamlit==1.36.0
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0
plotly==5.22.0
PuLP==2.8.0
PyYAML==6.0.1
python-dotenv==1.0.1
SQLAlchemy==2.0.31
